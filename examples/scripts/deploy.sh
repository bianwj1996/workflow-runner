#!/bin/bash
# Deploy script — replace with actual deploy commands
WORK_DIR="${1:-.}"
echo "Deploying from $WORK_DIR..."
# scp dist/* user@server:/app/
# ssh user@server "pm2 restart app"
echo "Deploy completed."
