#!/bin/bash
# Smoke test script — replace with actual test commands
WORK_DIR="${1:-.}"
echo "Running smoke tests in $WORK_DIR..."
# curl -f http://localhost:8080/health || exit 1
# curl -f http://localhost:8080/api/users || exit 1
echo "Smoke tests passed."
